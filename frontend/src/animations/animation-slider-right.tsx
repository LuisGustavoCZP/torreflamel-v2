import styled, {keyframes} from "styled-components";

export const sliderRight = keyframes`
    0% {
        transform: translate(100%, 0);
    }
    100% {
        transform: translate(0%, 0);
    }
`;